language: r
cache: packages
dist: xenial
addons:
  apt:
    packages:
      - libnode-dev
r_github_packages:
  - mrc-ide/dust@i22-no-rcpp
r_packages:
  - covr
after_success:
  - Rscript -e 'covr::codecov(quiet = FALSE)'
